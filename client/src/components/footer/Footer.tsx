import styles from "../footer/styles.module.scss";
import React from "react";

const Footer = () => {
    return (
        <footer className={styles.footer}>
           <p>Bihun Sasha web app</p>
        </footer>
    );
};

export default Footer